[Linux vi/vim | 菜鸟教程](https://www.runoob.com/linux/linux-vim.html)
命令模式
- 进入插入模式
	- `i` 进入插入模式
	- `I` 在当前行的第一个非空格符处开始输入
	- `a` 在当前光标所在的下一个字符处插入
	- `A` 在当前尾行插入

命令行模式
- `Esc` 退出插入模式，回到普通模式
- `:w` 保存文件
- `:q` 退出Vim编辑器
- `:q!` 强制退出Vim编辑器
- `:wq` 保存文件并退出Vim编辑器
- `:e<file>` 打开一个文件
- `:e!` 重新加载当前文件